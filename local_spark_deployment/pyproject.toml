[project]
name = "local-spark-deployment"
version = "0.1.0"
description = "Docker-based local Spark 3.5 cluster deployment for development"
requires-python = ">=3.10"
dependencies = [
    "pyspark==3.5.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
